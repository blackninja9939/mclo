#pragma once

/// @brief Expands to nothing.
/// @details Handy as the "empty" branch of conditional macros such as @ref MCLO_IF, where an argument is required
/// but no output is wanted.
#define MCLO_EMPTY
